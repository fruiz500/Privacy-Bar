/**
    * THE GATEKEEPER: Standardized check for Pro features
    * Returns true if Pro is active, otherwise shows the modal and returns false.
    */

///  pro status checking is disabled for GitHub. See the actual code in the extension's console, after installation.

window.checkProGate = function(){ return true };